<div class="mx-auto col-md-5 text-center d-flex align-items-center">
  <%= image_tag member.get_profile_image, class: "not-link-image rounded-circle" %>
</div>
<table class="table table-borderless text-white col-md-6 offset-md-1">
  <tbody>
    <tr>
      <td>名前</td>
      <td class="h4"><%= member.name %></td>
    </tr>
    <tr>
      <td>レベル</td>
      <td>Lv.<%= member.level %></td>
    </tr>
    <tr>
      <td>経験値</td>
      <td><%= member.exp %></td>
    </tr>
    <tr>
      <td>マネー</td>
      <td>
        <% if member.money.to_i > 0 %>
          <%= member.money %>
        <% else %>
          0
        <% end %>
      </td>
    </tr>
    <tr>
      <td>お題を投稿した数</td>
      <td><%= member.boards.count %></td>
    </tr>
    <tr>
      <td>答えを投稿した数</td>
      <td><%= member.posts.count %></td>
    </tr>
    <tr>
      <td>次のレベルまでの必要経験値</td>
      <td><%= gap_exp %></td>
    </tr>
  </tbody>
</table>