<div class="row">
  <div class="col-lg-8">
    <% @boards.each do |board| %>
      <%= render "show", board: board %>
    <% end %>
  </div>
  <div class="col-lg-4">
    <%= render "all_tag", tags: @tags %>
  </div>
</div>