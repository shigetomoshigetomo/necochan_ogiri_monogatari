<div class="text-center my-2"><%= link_to "アイテム詳細へ", admin_item_path(@item), class: "btn btn-info btn-sm" %></div>
<div class="box">
  <%= form_with model:@item, url: admin_item_path(@item), method: :patch do |f| %>
    <div class="form-group row">
      <%= f.label :商品画像, class: "col-3" %>
      <%= f.file_field :image, accept: "image/*", class:"col-8 p-0" %>
    </div>
    <div class="form-group row">
      <label class="col-3">商品名</label>
      <label class="col-8"><%= @item.name %></label>
    </div>
    <div class="form-group row">
      <%= f.label :商品説明, class: "col-3" %>
      <%= f.text_area :introduction, rows:"4", class:"col-8" %>
    </div>
    <div class="form-group row">
      <%= f.label :ジャンル, class: "col-3" %>
      <%= f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name), { prompt: '選択してください' } %>
    </div>
    <div class="form-group row">
      <label class="col-3">価格</label>
      <label class="col-8"><%= @item.price %></label>
    </div>
    <div class="form-group row">
      <label class="col-3">経験値</label>
      <label class="col-8"><%= @item.having_exp %></label>
    </div>
    <div class="form-group row mt-3">
      <div class="mx-auto">
        <%= f.submit "更新", class:"btn btn-outline-light btn-lg" %>
      </div>
    </div>
  <% end %>
</div>